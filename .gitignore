# Go
/luna
/bin/luna
/dist/
*.test
*.out
coverage.out

# Local state
*.db
*.db-shm
*.db-wal
/.luna/

# Secrets
config/*.env
!config/*.env.example

# System
.DS_Store

# Local developer tooling, not project configuration: the ai-jail sandbox marker
# and the durable-memory scope. Both exist in the worktree and are needed to work
# here — they just do not belong in the repository.
.ai-jail
.ai-memory.toml

# Wave 5 research: five cloned orchestrators (~410 MB) plus the reports written
# from them. Working material for a decision, not a layer of the docs suite — it
# is written in Portuguese and stays in the worktree for alignment and for
# borrowing ideas from the systems studied. docs/luna_study/README.md carries the
# SHAs that make it reproducible if it is ever deleted.
docs/luna_study/
