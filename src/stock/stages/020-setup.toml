# Mechanical: a worktree is git, and paying a model to make one buys nothing
# (ADR-0040). It names no role, and the static check allows that because a
# worktree is not a judgement.

id       = "setup"
requires = ["task_id"]
produces = ["worktree"]

[verify.worktree]
kind = "existence"
